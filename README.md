# React-Hangman
A replica of the popular guessing game 'Hangman'. Built with React JS.

## Installation
1. Clone the repo
2. Install NPM packages
 ```
 npm install
 ```
3. Run App
 ```
 npm start
 ```
